# parcel-plugin-i18n-pug

This plugin will look for occurences of `__('language string')`

Not working:

-   shared dependencies (includes and extends)
-   using the official parcel asset for pug (extending it)
-
